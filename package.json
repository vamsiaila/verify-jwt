{
  "name": "verify-jwt",
  "version": "1.0.8",
  "description": "a middleware used to verify jwt you just need to pass the jwt token through query params named token are headers named x-access-token",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "jsonwebtoken": "^8.4.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/vamsiaila/verify-jwt.git"
  },

  "keywords": [
    "verify-jwt",
    "verify jwt",
    "verify jsonwebtoken",
    "verify token",
    "jwt token verification",
    "jwt token",
    "vj",
    "jwt",
    "jsonwebtoken",
    "middleware"
  ]
}
